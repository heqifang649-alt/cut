# Semantic Evaluation Fixture v1

This synthetic fixture proves only the evaluation contract: metric accounting,
ground-truth mapping, repeatability, and telemetry aggregation. It contains no
media, provider calls, credentials, or semantic-quality claim.

The production P2 benchmark must replace it with independently labelled,
representative shots and recorded provider observations. Do not treat a fixture
pass as evidence that a model is semantically qualified.
